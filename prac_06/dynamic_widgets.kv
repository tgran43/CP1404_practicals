BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'vertical'
        # this layout is populated with (button) widgets in app code
        id: entriesBox
        cols: 1
        orientation: 'vertical'

